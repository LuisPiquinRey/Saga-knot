spring:
    application:
        name: configs
    profiles:
        active: native
    cloud:
        bus:
            enabled: false
        config:
            server:
                native:
                    search-locations: "classpath:/config"
                encrypt:
                    enabled: true
    rabbitmq:
        host: rabbitmq
        port: 5672
        username: guest
        password: guest
encrypt:
    key: myS1c_retKey123456

server:
    port: 8090
    