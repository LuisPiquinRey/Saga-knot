spring:
    application:
        name: discovery
    config:
        import: configserver:http://configs:8090/
    cloud:
        config:
            uri: http://configs:8090
            fail-fast: true
